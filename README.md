# front-end

DOCKER

**installazione vite per vue**

sudo npm install -g

sudo npm i



docker compose up --build --force-recreate



kubernetes

minikube start
kubectl apply -f front-end.yaml
minikube tunnel
